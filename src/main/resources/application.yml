spring:
  application:
    name: startup_curso
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/startup_curso?useTimezone=true&serverTimezone=UTC
    username: root
    password: Root@1234
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
    show-sql: true
    open-in-view: false
logging:
  level:
    simao.com.startup: DEBUG
